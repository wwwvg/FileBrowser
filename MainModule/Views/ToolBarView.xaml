<UserControl x:Class="MainModule.Views.ToolBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <ToolBar>
            <Button Name="ButtonNewFolder" Command="{Binding AddFolder}" Width="25" Height="25" Margin="5, 0, 0, 0" ToolTip="Добавить каталог">
                <Image Source="{Binding AddFolderImage}" />
            </Button>
            <Button Name="ButtonDeleteItem" Command="{Binding DeleteItem}" Width="25" Height="25" Margin="10, 0, 0, 0" ToolTip="Удалить выбранный элемент">
                <Image Source="{Binding DeleteItemImage}"/>
            </Button>
        </ToolBar>
    </Grid>
</UserControl>
