<UserControl x:Class="MainModule.Views.Content.HexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <ScrollViewer>
            <TextBox Text="{Binding Text}" FontFamily="Courier New" IsReadOnly="True"></TextBox>
            <!--<RichTextBox FontFamily="Courier New">
                <FlowDocument PageHeight="180">
                    <Paragraph>
                        <Run Text="{Binding Text, Mode=TwoWay}"/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>-->
            <!--<i:Interaction.Triggers>
                <i:EventTrigger EventName="ScrollChanged">
                    <prism:InvokeCommandAction Command="{Binding ScrollChanged}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>-->
        </ScrollViewer>
    </Grid>
</UserControl>
